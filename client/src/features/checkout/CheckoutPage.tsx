import { Typography } from "@mui/material";

const CheckoutPage = () => {
  return <Typography variant="h3">Only Logged in user should be able to see this</Typography>;
};

export default CheckoutPage;
